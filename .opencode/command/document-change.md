---
description: Update doc/spec and archive a completed doc/todo change under doc/done.
---

Use the `document-change` skill for change request `$ARGUMENTS`. Require complete passing implementation evidence, update durable documentation, ensure every request link targets `doc/done`, and archive with `git mv`.
