# Incoming Change Requests

Each new change request lives in its own directory named `<id>-<change-request-title>`.

The request's `README.md` is the immutable original request. The spec-driven workflow adds `amendment.md`, `specification.md`, `implementation-plan.md`, and `implementation-notes.md` as applicable. Completed requests are moved to [`doc/done/`](../done/).
